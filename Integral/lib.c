#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "lib.h"

double integral(double a, double b, double* f(double)){
  double result;
  result = f(a) + f(b);
  
}

double divide(double a, double b, double* f(double)){
  return 0;
}